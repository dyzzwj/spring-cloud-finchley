spring:
  application:
    name: eureka-server
server:
  port: 1113
eureka:
  instance:
    hostname: peer3
  client:
    service-url:
      defaultZone: http://peer1:1111/eureka/,http://peer2:1112/eureka/  #map类型 多个逗号隔开 指向peer1和peer2
